<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>أداة الإرسال البسيطة (اختبار حاسم)</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700&display=swap' );

        :root {
            --primary-color: #2563eb;
            --secondary-color: #f0f9ff;
            --text-color: #1e293b;
            --border-color: #cbd5e1;
            --success-color: #16a34a;
            --error-color: #dc2626;
        }

        body {
            font-family: 'Tajawal', sans-serif;
            background-color: var(--secondary-color);
            color: var(--text-color);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            padding: 1rem;
        }

        .container {
            background: #ffffff;
            padding: 2.5rem;
            border-radius: 12px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
            width: 100%;
            max-width: 600px;
            text-align: center;
            border-top: 5px solid var(--primary-color);
        }

        .logo {
            font-size: 2.5rem;
            margin-bottom: 0.5rem;
            color: var(--primary-color);
        }

        h1 {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }

        p.subtitle {
            color: #64748b;
            margin-bottom: 2rem;
        }

        textarea {
            width: 100%;
            height: 160px;
            padding: 1rem;
            border: 1px solid var(--border-color);
            border-radius: 8px;
            margin-bottom: 1.5rem;
            font-size: 1rem;
            font-family: 'Tajawal', sans-serif;
            resize: vertical;
        }
        
        button {
            width: 100%;
            padding: 1rem;
            font-size: 1.1rem;
            font-weight: 700;
            color: #fff;
            background-color: var(--primary-color);
            border: none;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 0.5rem;
        }
        button:disabled {
            background-color: #94a3b8;
            cursor: not-allowed;
        }

        .status {
            margin-top: 1.5rem;
            font-weight: bold;
            padding: 0.75rem;
            border-radius: 6px;
            display: none;
        }
        .status.success { background-color: #dcfce7; color: var(--success-color); display: block; }
        .status.error { background-color: #fee2e2; color: var(--error-color); display: block; }
        .status.info { background-color: #dbeafe; color: var(--primary-color); display: block; }

    </style>
</head>
<body>
    <div class="container">
        <div class="logo">✉️</div>
        <h1>أداة الإرسال البسيطة</h1>
        <p class="subtitle">اكتب أي شيء في المربع أدناه. سيتم إرساله كبريد إلكتروني.</p>
        
        <textarea id="text-input" placeholder="اكتب هنا..."></textarea>
        
        <button id="submit-button" onclick="sendData()">
            <span id="button-text">إرسال</span>
            <div id="spinner" style="display: none; border: 3px solid rgba(255,255,255,0.3); border-top: 3px solid #fff; border-radius: 50%; width: 20px; height: 20px; animation: spin 1s linear infinite;"></div>
        </button>
        
        <div id="status-message" class="status"></div>
    </div>

    <script>
        // ==================================================
        // !!! تم دمج الرابط الذي أصررت عليه هنا !!!
        // ==================================================
        const N8N_WEBHOOK_URL = "https://ahmedbpnb.app.n8n.cloud/webhook/eecbce08-6888-4943-a176-1a49ff022654/chat";

        const textInput = document.getElementById('text-input' );
        const submitButton = document.getElementById('submit-button');
        const statusMessage = document.getElementById('status-message');
        const buttonText = document.getElementById('button-text');
        const spinner = document.getElementById('spinner');

        const style = document.createElement('style');
        style.innerHTML = `@keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }`;
        document.head.appendChild(style);

        function setStatus(message, type) {
            statusMessage.textContent = message;
            statusMessage.className = 'status ' + type;
        }

        async function sendData() {
            const text = textInput.value;

            if (!text.trim()) {
                setStatus('خطأ: النص فارغ.', 'error');
                return;
            }

            setStatus('جارٍ الإرسال...', 'info');
            submitButton.disabled = true;
            buttonText.style.display = 'none';
            spinner.style.display = 'block';

            try {
                const response = await fetch(N8N_WEBHOOK_URL, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ "text": text }) // تم التبسيط ليتوافق مع سير العمل البسيط
                });

                if (response.ok) {
                    setStatus('نجاح! تم إرسال الطلب.', 'success');
                } else {
                    setStatus(`فشل. رمز الخطأ: ${response.status}`, 'error');
                }

            } catch (error) {
                setStatus('خطأ في الشبكة.', 'error');
            } finally {
                submitButton.disabled = false;
                buttonText.style.display = 'inline';
                spinner.style.display = 'none';
            }
        }
    </script>
</body>
</html>
